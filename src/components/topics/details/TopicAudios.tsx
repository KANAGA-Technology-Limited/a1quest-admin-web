import React from 'react';
import { SingleTopicType } from '../../../types/data';

const TopicAudios = ({ data }: { data: SingleTopicType | undefined }) => {
  return <div>TopicAudios</div>;
};

export default TopicAudios;
